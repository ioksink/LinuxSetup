This vault is intent to note packages used in Ubuntu and be prepared to migrate to Fedora Workstation.

GNU or GNOME tools should be the same and other packages are likely managed by `dnf` or `flatpak`.

Further tests are required.
